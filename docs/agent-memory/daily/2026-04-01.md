# 2026-04-01

## Context
- Focus area: Shopify embedded app UX, readability, and API/manual testing support.
- Merchant store used during testing: `blackeagletest.myshopify.com`.
- Production server used for deploys: `167.172.60.234`.

## Decisions
- Use file-based daily memory to avoid losing important project context between tasks.
- Keep memory factual and short instead of long narrative notes.
- Track UX findings, deploys, and unresolved issues in the daily file.

## Changes
- Built a full API route inventory from the Hono app.
- Created API test scenario documentation.
- Improved Shopify app UX structure:
  - grouped navigation into setup vs daily work
  - changed overview into launch/readiness flow
  - improved dashboard prioritization
  - improved products flow around scrape and embeddings
  - replaced several floating popups with inline Polaris feedback
- Improved settings guardrail UX:
  - required fields are now clearer
  - submit is blocked until required values are present
- Improved shell readability and active navigation state:
  - reduced overly dark active sidebar styling
  - increased contrast in shared UI components

## Deployments
- Multiple production deploys were performed to `shopify-shell` on `167.172.60.234`.
- Latest deploy on this date includes:
  - sidebar active-state readability fix
  - shared shell contrast improvements

## Observed Issues
- Some external/bot requests hit random paths like `/.git/...`, `/static/style/...`, and `/wiki`, creating noisy `404` logs in `shopify-shell`.
- These logs did not indicate app boot failure.
- Merchant feedback: some screens still feel old despite structural UX improvements.
- Shopify shell does not own WhatsApp delivery logic; WhatsApp flow lives in `packages/api`, so embedded app testing is indirect.
- `packages/api/src/routes/test.ts` contains useful WhatsApp test endpoints, but they are not mounted in `packages/api/src/index.ts`.
- Inbound WhatsApp testing requires signed provider webhooks plus queue/worker flow, not just clicking inside Shopify.
- Outbound WhatsApp testing from the shell currently depends on an existing conversation and valid effective credentials.
- Merchant sender mode may be forced to `corporate` by plan rules even if the UI shows a sender mode selector.

## Open Items
- Continue visual modernization beyond structure cleanup.
- Reduce “old admin panel” feel by lowering card density and improving art direction.
- Review remaining text contrast edge cases in live usage.
- Keep app memory updated after major UX changes or deployments.
- Decide whether to expose a real WhatsApp test panel inside the Shopify shell or keep testing at API/provider level.
