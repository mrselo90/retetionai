# 2026-04-14

## Context
- Main focus: Shopify shell setup UX gap for `Orders flowing` checklist step.
- Environment: local monorepo + production server `167.172.60.234`.

## Decisions
- Keep existing checklist architecture; add explicit action guidance instead of redesigning setup flow.
- Route `Continue setup` for `Orders flowing` directly to an anchored guidance section.

## Changes
- Updated `packages/shopify-app/app/routes/app.tsx`:
  - `Orders flowing` checklist detail now explains what merchant must do.
  - next-step route now points to `/app/integrations#orders-flow`.
  - button label for that step is now `Open order flow setup`.
- Updated `packages/shopify-app/app/routes/app.integrations.tsx`:
  - added new section `Orders flow setup` (`id="orders-flow"`).
  - shows active/pending status based on `overview.metrics.totalOrders`.
  - pending state now includes concrete step-by-step instructions and action buttons:
    - `Go to dashboard`
    - `Refresh order flow status`
  - active state shows confirmation card with order count.
- Build verification:
  - `pnpm --filter @recete/shopify-app build` passed.
- Fixed product setup wizard progression bug in `packages/shopify-app/app/routes/app.products._index.tsx`:
  - Root cause: Step 1 completion (`hasGuidance`) was derived from unsaved draft text.
  - Resulting issue: typing a single character in `Customer instructions` immediately marked step complete and auto-jumped to Step 2.
  - Fix: Step 1 now counts as complete only when guidance is persisted or draft is not dirty (i.e., saved), preventing accidental step jumps before `Continue` / `Save draft`.
- Verification:
  - `pnpm --filter @recete/shopify-app build` passed after fix.
- Updated `packages/shopify-app/app/routes/app.tsx` to avoid duplicate progress indicators on product setup detail pages:
  - hides left-sidebar `Launch progress` card when route is `/app/products` with a selected product query.
  - keeps step progress bar only in the main setup panel for that screen.
- Verification:
  - `pnpm --filter @recete/shopify-app build` passed after UI change.
- Updated `packages/shopify-app/app/routes/app.tsx` to avoid duplicate loading indicators:
  - global shell spinner now renders only when `navigation.state === "loading"` (not on `submitting`).
  - prevents double spinner on product setup while page-level submitting spinner is active.
- Verification:
  - `pnpm --filter @recete/shopify-app build` passed after loader change.
- Updated `packages/shopify-app/app/routes/app.products._index.tsx`:
  - added `Extra source URL` field to Step 3 (`Languages`) so merchants can still provide source URL when Step 2 is auto-completed.
  - root cause observed: Step 2 completion is currently tied to `hasAiKnowledge` (`done: hasAiKnowledge`), so existing knowledge can skip direct Step 2 editing.
- Verification:
  - `pnpm --filter @recete/shopify-app build` passed.
- Refactored product setup flow in `packages/shopify-app/app/routes/app.products._index.tsx` to a true 2-step wizard:
  - Step 1: `Customer instructions`
  - Step 2: `Enhancement` (extra source URL + AI knowledge status + language coverage)
  - removed separate Step 3 block and merged language context into Step 2.
  - wizard editor now keeps Step 2 accessible after Step 1 instead of forcing a hidden/auto-skipped intermediate stage.
- Verification:
  - `pnpm --filter @recete/shopify-app build` passed after refactor.
- Fixed duplicated action controls in Step 2 (`Enhancement`) in `packages/shopify-app/app/routes/app.products._index.tsx`:
  - removed duplicated upper `Continue / Save draft` button group.
  - kept a single action button group at the bottom of the step.
- Verification:
  - `pnpm --filter @recete/shopify-app build` passed.
- Fixed Step 2 language coverage regression trigger in `packages/shopify-app/app/routes/app.products._index.tsx`:
  - Root cause: `Continue` in Step 2 always triggered embeddings pipeline, even when enhancement was already complete and no extra source URL was provided.
  - Symptom: coverage could drop from `3/3` to `0/3` after pressing `Continue` on an already-ready product.
  - Fix: added `enhancementNeedsRun` guard; primary button now:
    - runs embeddings only when needed (`!enhancementDone` or URL provided),
    - otherwise acts as `Done` and exits to product list without rerunning pipeline.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Refactored product setup UI to single-page flow in `packages/shopify-app/app/routes/app.products._index.tsx`:
  - removed step-oriented editing flow from setup panel and merged `Customer instructions` + `Enhancement` into one screen.
  - removed product subtitle line that showed `vendor • /handle` in setup header and product list rows.
  - added bulk operations in product list:
    - per-row selection checkboxes
    - `Select all shown` / `Clear`
    - `Update AI for selected` bulk action (`intent: bulk-prepare`) with server-side loop via `prepareMerchantProductKnowledge`.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Fixed regressions introduced by single-page refactor in `packages/shopify-app/app/routes/app.products._index.tsx`:
  - restored always-visible `AI knowledge` and `Preview answer` sections under setup form.
  - fixed preview action payload key from `previewQuestion` to `question` so backend preview endpoint receives the prompt correctly.
  - kept single-page setup behavior while restoring legacy product Q&A visibility.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Improved danger zone UX clarity in `packages/shopify-app/app/routes/app.products._index.tsx`:
  - replaced plain `Show danger zone` link-style action with a dedicated warning container.
  - added explicit explanation text next to action button describing destructive impact.
  - kept destructive controls behind explicit open/confirm flow.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Improved product detail page focus in `packages/shopify-app/app/routes/app.products._index.tsx`:
  - while a product is selected, `All products` list is now hidden to avoid mixed/overlapping visual flow.
  - users return to list via existing `← All products` action.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Updated ready-products visibility default in `packages/shopify-app/app/routes/app.products._index.tsx`:
  - `Ready products` list now loads expanded by default (no automatic initial hide).
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Updated product setup requirement behavior in `packages/shopify-app/app/routes/app.products._index.tsx`:
  - removed hard requirement for `Customer instructions` during save.
  - products can now be saved without instructions (for draft/setup continuity).
  - added explicit `Missing instructions` badge in product list rows for visibility.
  - improved bulk-update error text to explain next action when setup is not yet saved.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Applied UX cleanup for products setup page (`packages/shopify-app/app/routes/app.products._index.tsx`):
  - removed duplicate mid-form action buttons; only bottom action set remains.
  - standardized bottom actions to `Save & Update AI` (primary) and `Save Draft` (secondary).
  - removed extra attention badges from info/progress sections; top header status badge remains.
  - converted plain helper text under header into a full-width info banner with icon.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Refactored Billing + shell sidebar UI per Polaris cleanup request:
  - `app.tsx`:
    - removed descriptive text under `Merchant workspace`.
    - removed `Open dashboard` button from `Launch progress` card (prevents duplicate with Operations > Dashboard).
  - `app.billing.tsx`:
    - removed `Change plan in Shopify` section card.
    - updated `Current subscription` empty state to include primary `Choose a Shopify plan` button linking to Shopify managed pricing (`target="_top"`), while keeping illustration + `No active subscription`.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Redesigned Billing page for conversion-first flow in `packages/shopify-app/app/routes/app.billing.tsx`:
  - removed redundant warning/info banners and decision blocks.
  - moved plan selection focus to top-level compare cards.
  - added compact status card (`Current Status`) with `Free / No Plan` or active plan badge.
  - added per-tier primary CTA buttons (`Choose Starter/Growth/Pro`) linking to Shopify managed pricing with plan query.
  - added lightweight billing support link at bottom.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Deployed commit `175ff24` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
- Build fix for Billing redesign:
  - removed client-route dependency on `shopify.server` from `app.billing.tsx` (React Router server-only import guard).
  - replaced plan key references with client-safe local constants (`starter-monthly`, `growth-monthly`, `pro-monthly`, etc.).
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Deployed commit `90c54c2` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
- Removed confirmed dead/unnecessary code from products route (`packages/shopify-app/app/routes/app.products._index.tsx`):
  - deleted unused legacy components: `ProductListItem`, `JourneyStepper`.
  - removed unused state/props paths: `saveFeedback`, `inlineValidationError`, `actionIntent`, `showReadyEditor`.
  - removed unused helpers/types/imports: `formatSavedAt`, `languageLabel`, `ReactNode` import.
  - removed unused derived values (`selectedJourney`, `isSingleTaskMode`) after single-page/list UX changes.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Fixed bulk-update navigation regression in products route:
  - `bulk-prepare` action no longer returns `selectedProductId`.
  - prevents auto-navigation into product detail after bulk submit; user remains on list context.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Deployed commit `44105dc` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
- Deployed commit `cd3c7bf` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
- Deployed commit `de844e5` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
- Fixed backend validation mismatch for optional instructions:
  - Updated `packages/api/src/schemas/products.ts` so `usage_instructions` no longer enforces `.min(1)`.
  - This aligns API validation with Shopify app behavior where instructions are optional.
- Updated UI helper text for `Customer instructions` to explicitly mark it optional in:
  - `packages/shopify-app/app/routes/app.products._index.tsx`
- Verification:
  - `pnpm --filter @recete/api typecheck` passed.
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Deployed commit `fcc9fc7` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/api build`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart api web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
- Deployed commit `26e5fdc` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
- Deployed commit `6d6183e` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.

## Deployments
- Deployed commit `a2f3dc1` to production (`167.172.60.234`) via `./deploy.sh`.
- PM2 services after deploy: `api`, `web`, `shopify-shell`, `workers` all `online`.
- Deployed commit `e0881d6` to production (`167.172.60.234`) via `./deploy.sh`.
- PM2 services after deploy remained online (`api`, `web`, `shopify-shell`, `workers`).
- Deployed commit `bd5b5f1` to production (`167.172.60.234`) via `./deploy.sh`.
- PM2 services after deploy remained online (`api`, `web`, `shopify-shell`, `workers`).
- Deployed commit `a947e5a` to production (`167.172.60.234`) via `./deploy.sh`.
- PM2 services after deploy remained online (`api`, `web`, `shopify-shell`, `workers`).
- Deployed commit `ca48dfa` to production (`167.172.60.234`) via `./deploy.sh`.
- PM2 services after deploy remained online (`api`, `web`, `shopify-shell`, `workers`).
- Deployed commit `4014762` to production (`167.172.60.234`) via `./deploy.sh`.
- PM2 services after deploy remained online (`api`, `web`, `shopify-shell`, `workers`).
- Deployed commit `5582ca9` to production (`167.172.60.234`) via `./deploy.sh`.
- PM2 services after deploy remained online (`api`, `web`, `shopify-shell`, `workers`).
- Deployed commit `b7e69e5` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
- Deployed commit `8db66c9` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
- Deployed commit `fc6a61c` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
- Deployed commit `523d3a0` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
- Deployed commit `ec4db75` to production (`167.172.60.234`) via SSH rollout:
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.

## Observed Issues
- Existing non-blocking build warnings remain (sourcemap warnings in shopify-app, known Next/Sentry warnings in web).

## Open Items
- Merchant-side validation: confirm new `Orders flow setup` section is visible after clicking `Continue setup`.
- If needed, add deep link to Shopify Admin Orders for faster merchant action in a follow-up.
- Merchant-side validation: confirm Product Setup Step 1 no longer auto-advances while typing unsaved instructions.
- Fixed `Save & Update AI` disabled regression on product detail (`packages/shopify-app/app/routes/app.products._index.tsx`):
  - root cause: primary action was disabled whenever `enhancementNeedsRun` and `row.localProduct` was missing, blocking first-time save/update.
  - fix: added `handlePrimarySaveAndUpdate()` to run `save-setup` when product setup is dirty or local product missing, run embeddings only when appropriate, and only exit when no action needed.
  - primary button now disables only on active process lock and shows loading for both save and AI run.
- Fixed duplicate loader on Billing page (`packages/shopify-app/app/routes/app.billing.tsx`):
  - removed route-level `useNavigation` spinner; shell-level loading indicator remains authoritative.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
