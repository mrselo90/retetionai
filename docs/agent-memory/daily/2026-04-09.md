# 2026-04-09

## Context
- Main focus: Fix the Products page language coverage action so merchants see progress and the screen updates without manual reloads.
- Environment: local workspace (`retention-agent-ai`), Shopify embedded app production route.

## Decisions
- Treat `Complete language coverage` as a background refresh flow in the UI, not an instant final-state action.
- Auto-revalidate the Products route for a short period after embeddings/language refresh starts so language coverage can update on screen without manual refresh.

## Changes
- Updated `packages/shopify-app/app/routes/app.products._index.tsx`:
  - persisted recent embeddings action per product with timestamps
  - added automatic route revalidation polling after `embeddings` actions
  - added cleanup for completed/expired language refresh states
  - changed product row and setup panel copy/badges to show `Refreshing` / `Language refresh in progress`
  - improved inline feedback for `Complete language coverage` so the merchant sees that work is running
- Updated `packages/shopify-app/app/routes/app.products._index.tsx` again:
  - pinned the selected product into a dedicated `Currently editing` section above the grouped lists
  - excluded the selected product from the lower grouped sections to stop it jumping to another area after each completed step
  - result: the open setup form stays in a stable place while the merchant continues editing
- Redesigned the Product Setup Detail experience in `packages/shopify-app/app/routes/app.products._index.tsx`:
  - changed the selected product view from an embedded list/detail panel into a focused single-column workflow
  - added a sticky product context header with persistent product identity, step context, back navigation, and primary CTA
  - added dedicated workflow components for the active step, AI knowledge, missing information, preview answers, and a secondary product browser
  - kept existing save, embeddings refresh, preview answer, and delete logic intact while reducing competing actions on screen
- Hotfix in `packages/shopify-app/app/routes/app.products._index.tsx` after production feedback:
  - fixed the workflow header/step CTA to submit the embeddings action directly instead of relying on DOM lookup
  - fixed product detail close behavior so `All products` can actually dismiss the open product instead of auto-reopening one immediately
- Deeper production investigation for `Updating languages`:
  - observed that the in-step Languages/AI buttons were still using the older native form-submit path while only the header CTA had been moved to explicit `useSubmit`
  - shell logs showed recent `/app/products.data` reloads but no matching recent `POST /app/products` evidence around the problematic flow
  - updated the in-step Improve/Languages buttons to use the same explicit submit path as the workflow CTA so the UI cannot enter `Updating` without sending the request
- Tightened Product Setup Detail into a stricter single-task workflow:
  - while a product step is active, the page now hides the AI knowledge panel, missing info callout, preview panel, danger zone, and all-products browser
  - secondary panels are revealed only in the `ready` state, so the merchant sees one purpose per screen instead of mixed setup + insight UI
  - added a small locked-state message explaining that secondary panels unlock after the current task is completed
- Refined action hierarchy in Product Setup Detail:
  - removed duplicate primary CTA behavior from the sticky header and step summary card
  - converted header action area into status-only context with badge-based state
  - removed action-looking running states such as `Updating languages` as a button and replaced them with inline status + badge
  - limited the active step surface to one true primary CTA only when the system is waiting for input
- Adjusted `/app/products` default entry behavior:
  - removed automatic opening of the first product when the page loads without a `?product=` query param
  - result: merchants now land on the products list first and choose which product to open themselves
- Reduced repeated running-state noise in Product Setup Detail:
  - removed duplicate `Updating` messaging layers so running state is represented in a single surface instead of repeated badges/banners/inline blocks
- Product detail API inspection:
  - confirmed the detail flow ultimately calls `POST /api/products/:id/generate-embeddings` for AI answers and language coverage actions
  - recent production API log scan did not show matching `generate-embeddings` lines in the inspected tail window, so the suspicion that the action path is not reliably firing remains open
- Added temporary production diagnostics for the embeddings workflow:
  - instrumented the Shopify shell `embeddings` action in `packages/shopify-app/app/routes/app.products._index.tsx`
  - logs now capture action start payload, missing-product-id cases, the handoff before `generate-embeddings`, the response after it returns, and action-level failures
  - purpose: determine whether the break is missing form data, wrong intent routing, or failure before the internal API call
- Production investigation resolved the “stuck in progress” ambiguity:
  - latest reproduction proved `POST /api/products/:id/generate-embeddings` succeeds for product `b24cb4e9-df8b-4f75-b156-bb268e4a2926` (`Gift Card`)
  - DB check showed legacy `knowledge_chunks` exists for that product, but `product_i18n` and `knowledge_chunks_i18n` are both empty
  - merchant `shop_settings` has `enabled_langs = ["en","tr"]` but `multi_lang_rag_enabled = false`
  - result: the UI was waiting for multilingual readiness data that the backend is not currently producing for that shop, so the product could appear permanently `In progress`
- Updated `packages/shopify-app/app/routes/app.products._index.tsx` again:
  - added loader awareness for `multi_lang_rag_enabled`
  - changed product workflow state calculation so the Languages step only gates completion when multilingual coverage is actually enabled
  - filtered the step tracker to two steps when multilingual coverage is disabled
  - changed language status copy to explain primary-language readiness instead of implying a pending multilingual rebuild
- Reasserted multilingual as always-on system behavior:
  - changed `packages/api/src/lib/multiLangRag/config.ts` so chunk shadow writes default to enabled
  - changed `packages/api/src/lib/multiLangRag/shopSettingsService.ts` to force `multi_lang_rag_enabled` true in memory and auto-heal stale false rows in `shop_settings`
  - fixed `packages/api/src/lib/multiLangRag/chunkShadowWriteService.ts` to stop selecting non-existent `products.multilang_specs_json` / `products.multilang_faq_json` columns on production
  - result: multilingual shadow sync now runs successfully against the current production schema
- Product decision after repeated language-step failures:
  - removed Languages as a blocking setup step in the Shopify product workflow
  - new setup model is now two-step only: `Customer info` -> `AI answers` -> `Ready`
  - language coverage is now a secondary ready-state capability panel instead of a completion gate
  - rationale: setup must be deterministic; multilingual sync is asynchronous and should not block product readiness
- Added handoff document for external continuation:
  - `docs/ux/product-setup-handoff-2026-04-09.md`
  - includes current UX model, production findings, backend multilingual fixes, deploy state, and next-step guidance
- Post-handoff continuation (second agent session, 2026-04-09T23:25 local):
  - reviewed full `app.products._index.tsx` against handoff spec
  - found `StickyProductContextHeader` badge still showed `Step X of 3` instead of `Step X of 2`
  - fixed line 2423: replaced hardcoded `3` with `journey.totalSteps` so the step counter is driven by the 2-step `getJourneyMeta` return value
  - all backend API files (`config.ts`, `shopSettingsService.ts`, `chunkShadowWriteService.ts`) confirmed correct — no further changes needed
  - confirmed: Languages is NOT in `stepDefinitions`, NOT in `JourneyStepper`, NOT tracked in `getJourneyMeta`, secondary-panel only — model is clean
- Validation:
  - `pnpm --filter @recete/shopify-app typecheck`
  - `pnpm --filter @recete/api typecheck`
  - `pnpm --filter @recete/api test -- --run src/test/unit/middleware-response.test.ts`
  - Result: passed

## Deployments
- Production deploy completed for Products language coverage refresh fix:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart:
    - `pnpm --filter @recete/shopify-app build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed for selected-product stability fix:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart:
    - `pnpm --filter @recete/shopify-app build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed for Product Setup Detail workflow redesign:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart on `167.172.60.234`:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed for Product Setup Detail CTA/close hotfix:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart on `167.172.60.234`:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed for explicit embeddings-submit hotfix:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart on `167.172.60.234`:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed for stricter single-task Product Setup Detail flow:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart on `167.172.60.234`:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed for Product Setup Detail hierarchy cleanup:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart on `167.172.60.234`:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed for products-list-first default behavior:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart on `167.172.60.234`:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed for running-state noise cleanup:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart on `167.172.60.234`:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed for embeddings diagnostics logging:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart on `167.172.60.234`:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed for always-on multilingual backend:
  - synced files:
    - `packages/api/src/lib/multiLangRag/config.ts`
    - `packages/api/src/lib/multiLangRag/shopSettingsService.ts`
    - `packages/api/src/lib/multiLangRag/chunkShadowWriteService.ts`
    - `packages/api/src/test/unit/middleware-response.test.ts`
  - build + restart on `167.172.60.234`:
    - `cd /root/retetionai/packages/api && pnpm build`
    - `pm2 restart api`
    - `pm2 status api`
  - Result: `api` online after restart.
- Production data fix completed for multilingual enablement:
  - updated all `shop_settings` rows with `multi_lang_rag_enabled != true` to `true`
  - one-off shadow sync run for product `b24cb4e9-df8b-4f75-b156-bb268e4a2926` (`Gift Card`)
  - verification:
    - `product_i18n` now has `en` and `tr`
    - `knowledge_chunks_i18n` now has chunks for `en` and `tr`

## Observed Issues
- Language coverage rebuild is not instant; backend knowledge/language readiness updates after the embeddings action completes, so the UI needs explicit in-progress feedback.
- First workflow redesign regression:
  - large detail CTA showed the in-progress banner without reliably posting the embeddings action
  - `All products` / close behavior immediately auto-selected another product, so the detail view could not be dismissed
- Remaining risk:
  - if the explicit-submit fix still leaves language coverage unchanged, the next suspect is the async multi-language shadow-write pipeline (`shadowSyncProductKnowledge`) rather than the Shopify shell UI submit layer
  - current evidence still suggests the failure may occur inside the shell action before `POST /api/products/:id/generate-embeddings`; the new logs should confirm the exact break point on the next reproduction
  - current evidence now points to configuration / downstream multilingual readiness, not the button or embeddings request path
  - multilingual shadow sync had a production-schema mismatch (`multilang_specs_json` / `multilang_faq_json` missing) that is now fixed in code and production

## Open Items
- If language coverage still does not advance after polling, inspect production logs for the downstream multilingual rebuild path rather than the Shopify shell UI.
- Validate the new sticky workflow layout inside the Shopify embedded iframe to confirm scroll behavior and CTA visibility feel correct in production.
- Step-counter fix (`of 3` → `of 2`) is local only; needs a production deploy when convenient:
  - `pnpm --filter @recete/shopify-app build` → `pm2 restart shopify-shell`
- If desired: backfill multilingual shadow sync for all existing products beyond Gift Card.
