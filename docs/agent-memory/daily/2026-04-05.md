# 2026-04-05

## Context
- Main focus: Redesign `/app/products` as a cardless layout.
- Environment: local workspace (`retention-agent-ai`), Shopify embedded app route `app.products._index.tsx`.

## Decisions
- Remove all card-based presentation from Products page (`Card`, `SectionCard`, `MetricCard`) and switch to list/section layout using `Box`.
- Keep existing business logic, loader/action behavior, and route flow intact.

## Changes
- Updated `packages/shopify-app/app/routes/app.products._index.tsx`:
  - Replaced card-based metric blocks with border `Box` metric rows.
  - Replaced `Start Here` card section with a cardless bordered section and step boxes.
  - Replaced `Product pipeline` card section with a cardless section and vertical product rows.
  - Replaced product item container from `Card` to bordered `Box` row.
  - Replaced loading skeleton card wrapper with bordered `Box`.
  - Removed imports and usage of `Card`, `SectionCard`, and `MetricCard`.

## Deployments
- Local validation:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Production deploy completed:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.

## Observed Issues
- No runtime blocker observed; only expected sourcemap warnings during build.

## Open Items
- Validate visual density and spacing with merchant in embedded Shopify on desktop/mobile.

## Decisions
- Replaced split `Products` + `Mapping` UX with one unified Shopify product setup workspace on `/app/products`.
- Kept `/app/products/mapping` only as compatibility redirect to `/app/products?product=...`.
- Preserved existing backend contracts and product action APIs; merged frontend workflow into one route/action surface.

## Changes
- Rebuilt `packages/shopify-app/app/routes/app.products._index.tsx` as a master-detail workspace:
  - loader now fetches Shopify catalog, merchant products, instructions, and plan snapshot together.
  - left pane: searchable/filterable Shopify product list with status, progress, next action.
  - right pane: inline setup panel with product summary, required customer guidance, optional details, AI preparation actions, and readiness checklist.
  - replaced merchant-facing `mapping` language with `product setup`, `customer guidance`, `ready for AI`, `collect details`.
  - kept manual source entry as a secondary modal action.
- Replaced `packages/shopify-app/app/routes/app.products.mapping.tsx` UI with redirect-only compatibility route.

## Deployments
- Local validation:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Production deploy completed:
  - synced files:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
    - `packages/shopify-app/app/routes/app.products.mapping.tsx`
  - build + restart:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.

## Decisions
- Kept the unified `/app/products` flow and focused only on friction reduction, not another structural redesign.
- Made the setup panel action-first:
  - `Save guidance` is always the primary action.
  - `Mark as ready` is separate and secondary.
  - `Improve answers` replaces merchant-facing `AI knowledge` wording.
- Moved destructive removal into a hidden danger zone with explicit confirmation.

## Changes
- Updated `packages/shopify-app/app/routes/app.products._index.tsx`:
  - reduced visible fields in the setup panel to guidance + main actions by default.
  - moved summary, tips/warnings, video URL, and extra source URL under collapsed `Optional details`.
  - rewrote user-facing copy from technical/system language to benefit-driven guidance.
  - added empty guidance prompt with example text for first-time setup.
  - removed visible destructive delete action from the main action row and added a confirmed danger zone.
  - simplified product row hints to setup-oriented status language (`Ready for responses`, `Add guidance to finish setup`, etc.).

## Deployments
- Local validation:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Production deploy completed:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.

## Observed Issues
- Production build still shows existing sourcemap warnings in `EmbeddedSessionTokenBoundary` and session token client files, but the build completes successfully.

## Open Items
- Validate the new lighter panel in embedded Shopify on desktop/mobile, especially action clarity and danger-zone discoverability.

## Findings
- Investigated merchant report that the Shopify embedded app was not reachable from the Products page.
- Production `shopify-shell` was online and serving:
  - `GET /app/products ... 200`
  - `POST /app/session-token ... 200`
  - `GET /app/bootstrap.data ... 200`
- Console errors reported by the merchant were Shopify telemetry requests blocked by the client:
  - `https://error-analytics-sessions-production.shopifysvc.com/`
  - `https://otlp-http-production.shopifysvc.com/v1/metrics`
- These blocked telemetry requests are not app route failures.
- Logs also contain unrelated bot/noise 404s for `/robots.txt`, `/wiki`, and `/.well-known/security.txt`.

## Open Items
- If the merchant still sees a blank or stuck screen, verify browser-side causes first:
  - stale bookmarked embedded URL
  - ad blocker / Brave Shields / privacy extension
  - cached admin frame state

## Observed Issues
- Build still shows existing sourcemap warnings in `EmbeddedSessionTokenBoundary`/`sessionToken.client`, but no deploy blocker.

## Open Items
- Validate the new unified workspace with merchant behavior on mobile and desktop, especially selection flow and inline save clarity.

## Decisions
- Simplified the unified workspace further so the product list is the primary surface and the setup panel is secondary.
- Removed `Prepare for AI` as a standalone conceptual section; AI generation is now a secondary action within the setup panel.
- Simplified merchant-facing setup state to `Needs setup`, `In progress`, `Ready`, based primarily on linked record + customer guidance.

## Changes
- Reworked `packages/shopify-app/app/routes/app.products._index.tsx` again:
  - summary reduced to a compact top stat bar
  - main content changed to product table/list first, setup panel second
  - setup panel flattened into one linear flow:
    - Customer guidance
    - Optional details (collapsed)
    - Final actions
  - removed the separate `Prepare for AI` section and folded `Generate AI knowledge` into the final action row
  - changed primary action labels to merchant-friendly intent (`Save guidance`, `Mark as ready`, `Generate AI knowledge`)
  - removed heavy nested panels and checklist-heavy layout from the previous version
- Kept `packages/shopify-app/app/routes/app.products.mapping.tsx` as redirect-only compatibility route.

## Deployments
- Local validation:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Production deploy completed:
  - synced files:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
    - `packages/shopify-app/app/routes/app.products.mapping.tsx`
  - build + restart:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
