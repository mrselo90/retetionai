# 2026-04-07

## Context
- Main focus: Prepare screencast workflow for Shopify shell application.
- Environment: local workspace (`retention-agent-ai`), Shopify embedded app production URL.

## Decisions
- Use the user's logged-in Chrome session for Shopify Admin capture rather than headless browser automation because embedded Shopify admin authentication is session-bound.
- Keep both original `.mov` and compressed `.mp4` outputs.

## Changes
- Created screencast outputs:
  - `docs/screencasts/recete-shopify-shell-2026-04-07.mov`
  - `docs/screencasts/recete-shopify-shell-2026-04-07.mp4`
- Recording covers main Shopify shell areas:
  - Products
  - Conversations
  - Dashboard
  - Settings
  - Overview
- Updated Shopify app billing copy:
  - `packages/shopify-app/app/routes/app.billing.tsx`
  - `packages/shopify-app/app/routes/app.dashboard.tsx`
  - replaced harsh `Billing still blocks launch` / `blocker` wording with `Choose a Shopify plan` and softer plan-selection guidance.
- Fixed Shopify billing return flow:
  - updated `packages/shopify-app/app/routes/app.tsx`
  - added parent `/app` loader redirect for context-less billing return URLs like `/app/billing?shop=...&charge_id=...`
  - redirects these document requests to `https://admin.shopify.com/store/{store}/apps/{appHandle}/app/...` instead of falling through to `/auth/login`.

## Deployments
- Production deploy completed:
  - synced files:
    - `packages/shopify-app/app/routes/app.billing.tsx`
    - `packages/shopify-app/app/routes/app.dashboard.tsx`
  - build + restart:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed:
  - synced file:
    - `packages/shopify-app/app/routes/app.tsx`
  - build + restart:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.

## Observed Issues
- `ffmpeg` AVFoundation device listing did not expose screen devices, so macOS `screencapture` was used.
- First `screencapture` attempt failed because macOS option syntax requires `-D1` and `-V75` forms.
- Shopify Managed Pricing returned to app domain as `/app/billing?shop=blackeagletest.myshopify.com&charge_id=...` without embedded `host/id_token` context.
- Before the fix this fell through to `/auth/login`, showing `Connect your Shopify store`.
- Smoke test after deploy:
  - `GET https://shop.recete.co.uk/app/billing?shop=blackeagletest.myshopify.com&charge_id=test`
  - returned `302 Location: https://admin.shopify.com/store/blackeagletest/apps/blackeagle/app/billing?...`

## Open Items
- Review video before sharing externally because it may contain Shopify admin/store data.
