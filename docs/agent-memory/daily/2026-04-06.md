# 2026-04-06

## Context
- Main focus: Shopify embedded app Products page bug investigation.
- Environment: local workspace (`retention-agent-ai`), Shopify embedded app route `app.products._index.tsx`.

## Decisions
- Fix `Improve answers` by removing the nested form inside the main product setup form.
- Use React Router `useSubmit` with explicit `FormData` for secondary actions so embedded session token fetch patch can attach Authorization.

## Changes
- Updated `packages/shopify-app/app/routes/app.products._index.tsx`:
  - imported `useSubmit`.
  - changed `InlineActionForm` to render a plain Polaris `Button` instead of a nested `<Form>`.
  - submits `intent`, `productId`, `shopifyProductId`, `productName`, `hasContent`, and `enrichmentUrl` via `useSubmit(formData, { method: "post" })`.
  - keeps button labels and backend action handling unchanged.
- Updated `packages/shopify-app/app/routes/app.conversations._index.tsx`:
  - added `Last message: date, time` to conversation cards in `Manual priority queue` and `All recent threads`.
  - sorted conversations by status priority, then latest `lastMessageAt`, then message count.
  - added safe date/time formatting fallback for missing or invalid timestamps.

## Deployments
- Local validation:
  - `pnpm --filter @recete/shopify-app typecheck` passed.
- Production deploy completed:
  - synced file:
    - `packages/shopify-app/app/routes/app.products._index.tsx`
  - build + restart:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.
- Production deploy completed:
  - synced file:
    - `packages/shopify-app/app/routes/app.conversations._index.tsx`
  - build + restart:
    - `cd /root/retetionai/packages/shopify-app && pnpm build`
    - `pm2 restart shopify-shell`
    - `pm2 status shopify-shell`
  - Result: `shopify-shell` online after restart.

## Observed Issues
- Merchant reports `Missing Shopify session token in Authorization header` when clicking `Improve answers` on `/app/products?index=&product=8121277415498`.
- Production logs showed the broken click path as a native document submit:
  - `POST /app/products?... 401`
- This matched the invalid nested form issue.
- Production logs also had successful embedded data requests:
  - `GET /app/products.data ... 200`
  - `POST /app/session-token ... 200`

## Open Items
- Ask merchant to hard refresh and retest `Improve answers`.
