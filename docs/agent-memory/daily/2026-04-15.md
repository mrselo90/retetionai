# YYYY-MM-DD

## Context
- Main focus:
- Environment:

## Decisions
- 

## Changes
- 

## Deployments
- 

## Observed Issues
- 

## Open Items
- 
## Context
- Main focus: add admin-panel way to clear all merchant data.
- Environment: local monorepo.

## Decisions
- Implement merchant-wide deletion trigger in Shopify admin settings (danger zone).
- Use existing GDPR permanent deletion endpoint (`/api/gdpr/delete`) for full wipe semantics.

## Changes
- Updated `packages/shopify-app/app/platform.server.ts`:
  - added `deleteMerchantDataFromAdminPanel(request)` that calls `DELETE /api/gdpr/delete` with `{ confirm: true, permanent: true }`.
- Updated `packages/shopify-app/app/routes/app.settings.tsx`:
  - added new action intent `wipe-merchant-data` with strict confirmation phrase `DELETE ALL MERCHANT DATA`.
  - added dedicated success/error banner handling for wipe action.
  - added new `Danger zone` section with destructive form and confirmation input.
  - button stays disabled until exact confirmation phrase is entered.

## Observed Issues
- None during typecheck.

## Open Items
- Deploy this change to production if approved.
## Decisions
- Revised wipe flow semantics: keep merchant identity row, wipe operational merchant data.

## Changes
- Updated `packages/api/src/lib/dataDeletion.ts`:
  - added `clearMerchantDataKeepMerchant(merchantId)` to delete merchant-scoped operational data while preserving merchant row.
  - resets mutable merchant settings/billing flags (`persona_settings`, `notification_phone`, `guardrail_settings`, subscription fields) without deleting merchant identity.
- Updated `packages/api/src/routes/merchants.ts`:
  - added `DELETE /api/merchants/me/data-reset` (requires `{ confirm: true }`).
- Updated `packages/shopify-app/app/platform.server.ts`:
  - `deleteMerchantDataFromAdminPanel()` now calls `/api/merchants/me/data-reset`.
- Updated `packages/shopify-app/app/routes/app.settings.tsx`:
  - danger zone messaging now explicitly says merchant creation record is preserved.

## Verification
- `pnpm --filter @recete/api typecheck` passed.
- `pnpm --filter @recete/shopify-app typecheck` passed.
## Deployments
- Committed `a07c4ea` (`Add merchant data reset that preserves merchant record`).
- Pushed to `origin/main` and deployed to production (`167.172.60.234`):
  - `git pull`
  - `pnpm --filter @recete/api build`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart api web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
## Changes
- Performed application performance baseline measurements (build + runtime latency).
- Build timings (local):
  - `@recete/api build`: real `4.56s`
  - `@recete/workers build`: real `1.07s`
  - `@recete/shopify-app build`: real `3.64s`
  - `@recete/web build`: real `24.22s`
- External endpoint latency (public):
  - `https://recete.co.uk/health`: avg `0.243s` (10 samples), min `0.205s`, max `0.311s`
  - `https://shop.recete.co.uk/auth/login`: avg `0.224s` (10 samples), min `0.190s`, max `0.272s`
- External short load tests (`ab`, local runner):
  - `https://recete.co.uk/health` (`n=200,c=20`): `51.11 req/s`, p95 `482ms`, max `592ms`
  - `https://shop.recete.co.uk/auth/login` (`n=200,c=20`): `43.04 req/s`, p95 `793ms`, max `899ms`
- Server-side process/runtime checks:
  - `pm2 list`: `api`, `web`, `shopify-shell`, `workers` all online.
  - Internal API health port discovery: `3002` returns `200` (`3001` returns `404`).
  - Internal API latency (`http://127.0.0.1:3002/health`, 10 samples): avg `0.046s`, min `0.033s`, max `0.079s`.

## Open Items
- Full authenticated-route performance profiling (Shopify embedded/session-token flows, API merchant-scoped endpoints) still pending.
- DB-level query latency profiling (Supabase query stats/EXPLAIN for top endpoints) still pending.
## Observed Issues
- Shopify admin actions were failing with `403 Forbidden: Invalid internal secret`.
- Root cause: API -> shopify-shell internal calls used `INTERNAL_SERVICE_SECRET`, while shell internal route guards prioritized `PLATFORM_INTERNAL_SECRET` and required exact single-value match.

## Changes
- Updated `packages/shopify-app/app/routes/internal.plan.tsx` and `packages/shopify-app/app/routes/internal.billing.usage.tsx`:
  - internal secret guard now accepts either configured secret (`PLATFORM_INTERNAL_SECRET` or `INTERNAL_SERVICE_SECRET`) instead of strict priority-only single comparison.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.

## Deployments
- Committed `0864405` (`Accept both internal secrets for shell internal routes`).
- Pushed to `origin/main` and deployed to production (`167.172.60.234`):
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed all services online.
## Decisions
- Dashboard was refactored from passive metrics-first layout to guided onboarding-first layout without changing loader/API contracts.

## Changes
- Updated `packages/shopify-app/app/routes/app.dashboard.tsx`:
  - Removed duplicate primary CTA pattern (`ShellPage.primaryAction`, blocker action, next-focus action).
  - Added single dominant hero CTA: `Activate Recete` in new top section (`Start using Recete`).
  - Replaced `Next focus` with `Next step` and actionable guidance text.
  - Replaced `System status` row view with onboarding checklist (`Setup progress`) using done/pending/locked indicators.
  - Improved terminology:
    - `Consent-active users` -> `Customers who allowed messages`
    - `Catalog rows` -> `Products ready`
    - `Response rate` -> `Reply performance`
  - Added helpful zero-value metric hints and fallback `—` for reply performance when unavailable.
  - Kept existing data bindings (`subscription`, `metrics`, `recentOrders`) and route contracts intact.

## Verification
- `pnpm --filter @recete/shopify-app typecheck` passed.
## Deployments
- Committed `9a0762c` (`Refactor dashboard to activation-first onboarding flow`).
- Pushed to `origin/main` and deployed to production (`167.172.60.234`):
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed all services online.
## Changes
- Refined dashboard into onboarding-first workspace with state split in `packages/shopify-app/app/routes/app.dashboard.tsx`:
  - incomplete setup state now renders `Getting started` experience with one primary CTA and dependency-aware steps.
  - merged onboarding guidance into a single current-step card (action + unlock rationale).
  - hides metrics/analytics widgets during incomplete setup and replaces with `Live dashboard preview` guidance.
  - complete setup state renders actual dashboard metrics + recent orders.
  - setup flow now tracks: plan activation -> products -> messaging config -> first order -> conversations.

## Verification
- `pnpm --filter @recete/shopify-app typecheck` passed.
## Changes
- Redesigned customers page into context-aware adaptive flow in `packages/shopify-app/app/routes/app.customers.tsx`.
- Loader now fetches merchant overview + customers together so UI can infer onboarding context safely.
- Added three explicit rendering states:
  - `onboarding_incomplete`
  - `ready_no_data`
  - `has_data`
- State handling rules:
  - `has_data`: `customers.length > 0`
  - `ready_no_data`: setup ready (`billing + products + messaging`) but no customers yet
  - `onboarding_incomplete`: setup dependencies missing
- Removed meaningless early-state zero metrics and replaced with guided explanatory sections.
- Added setup dependency panel with clear lock messages (plan, products, messaging, first order, conversations).
- Reduced duplicate CTA noise by driving one contextual primary action per state.
- Preserved rich customer analytics/actions only in `has_data` state.

## Verification
- `pnpm --filter @recete/shopify-app typecheck` passed.
## Deployments
- Committed `3a8e12f` (`Make customers page context-aware across onboarding/data states`).
- Pushed to `origin/main` and deployed to production (`167.172.60.234`):
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed all services online.
## Changes
- Redesigned analytics page into adaptive 4-state workspace in `packages/shopify-app/app/routes/app.analytics.tsx`.
- Added explicit state machine:
  - `onboarding_incomplete`
  - `ready_no_data`
  - `has_data_basic`
  - `has_data_pro`
- State logic now combines onboarding readiness (billing/products/messaging) + analytics signal availability + plan analytics tier.
- Removed fake zero-state analytics rendering in onboarding and replaced with dependency + preview guidance.
- Added empty-but-promising experience for ready/no-data state with soft placeholders and action guidance.
- Reworked basic-tier upgrade section from restriction messaging to value framing (deeper insights with Pro).
- Reduced CTA noise by using one contextual primary action and one action per section.
- Recommendations now derive from live signals (response rate and sentiment) instead of static generic blocks.

## Verification
- `pnpm --filter @recete/shopify-app typecheck` passed.
## Deployments
- Re-deployed current `main` (`321ac7e`) to production (`167.172.60.234`) on request.
- Steps run on server: `git pull` (already up to date), `pnpm --filter @recete/shopify-app build`, `pm2 restart web shopify-shell`, `pm2 list`.
- Result: `api`, `web`, `shopify-shell`, `workers` all online after restart.
## Changes
- Unified Customers + Analytics UX around a shared 3-state model: `onboarding_incomplete`, `ready_no_data`, `has_data`.
- Added reusable UI primitives in `shell-ui`: `SetupDependencyList`, `ValuePreview`, and `StatePanel.statusLabel` override.
- Removed top-level CTA duplication by keeping page-level `primaryAction` as the single dominant CTA and removing duplicated hero action usage on both pages.
- Customers page now:
  - shows instructional setup dependencies with `Completed / Current step / Locked` semantics,
  - removes fake onboarding metrics,
  - uses value preview cards in onboarding,
  - keeps data-driven sections only in `has_data`.
- Analytics page now:
  - uses the same 3-state model as Customers,
  - removes 4-state split in favor of `has_data` with plan-aware sub-sections,
  - keeps setup/no-data guidance separate from real metrics,
  - keeps upgrade messaging only in `has_data` for non-Pro merchants.

## Verification
- `pnpm --filter @recete/shopify-app typecheck` passed.
- `pnpm --filter @recete/shopify-app build` passed (existing sourcemap warnings persisted, no build failure).
## Deployments
- Deployed latest Customers/Analytics state-aware redesign to production (`167.172.60.234`).
- Steps: `git pull` (already up to date), `pnpm --filter @recete/shopify-app build`, `pm2 restart web shopify-shell`, `pm2 list`.
- Result: `api`, `web`, `shopify-shell`, `workers` all online.
## Changes
- Fixed analytics CTA duplication UX: removed duplicate in-panel action pattern and standardized single dominant page-level primary action.
- Applied shared state-aware UX cleanup across Customers/Analytics with consistent hero/dependency/preview flow.

## Deployments
- Deployed commit `ced09a2` to production (`167.172.60.234`).
- Steps: `git pull`, `pnpm --filter @recete/shopify-app build`, `pm2 restart web shopify-shell`.
- Result: `api`, `web`, `shopify-shell`, `workers` all online.
## Changes
- Performed UX-consistency audit across app pages with focus on duplicate CTA and state clarity.
- Removed duplicate CTA pattern from Conversations page by dropping in-panel action when page-level primary action exists.
- Removed redundant top-level primary action from Integrations page to reduce action noise and avoid billing CTA duplication.

## Verification
- `pnpm --filter @recete/shopify-app typecheck` passed.
## Decisions
- Main `/app` entry should be setup-first, not metrics dashboard, until onboarding is complete.
- Keep exactly one dominant CTA on setup entry page and remove shell-level duplicate launch CTAs for `/app`.
- Sidebar should switch to simplified onboarding nav while setup is incomplete.

## Changes
- Rebuilt `app._index` as onboarding-first setup page:
  - Hero: "Welcome to Recete" + 3-value bullets + readiness percent.
  - Step-based clickable checklist (billing, products, messaging, order flow) with `done / in progress / not started` statuses.
  - Single page-level primary CTA (`Continue setup`) before completion.
  - Removed early-stage KPI/analytics dashboard blocks from main entry.
- Updated `app.tsx` shell behavior:
  - removed top duplicate overview hero/CTA block for `/app` to avoid CTA competition.
  - hid sidebar launch progress card on `/app` to reduce duplication.
  - added simplified sidebar mode during incomplete setup: Setup (enabled), Dashboard (disabled), Settings (disabled).
  - kept full navigation after setup completion.

## Verification
- `pnpm --filter @recete/shopify-app typecheck` passed.
- `pnpm --filter @recete/shopify-app build` passed (existing sourcemap warnings persisted).
