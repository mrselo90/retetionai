# YYYY-MM-DD

## Context
- Main focus:
- Environment:

## Decisions
- 

## Changes
- 

## Deployments
- 

## Observed Issues
- 

## Open Items
- 
## Context
- Main focus: add admin-panel way to clear all merchant data.
- Environment: local monorepo.

## Decisions
- Implement merchant-wide deletion trigger in Shopify admin settings (danger zone).
- Use existing GDPR permanent deletion endpoint (`/api/gdpr/delete`) for full wipe semantics.

## Changes
- Updated `packages/shopify-app/app/platform.server.ts`:
  - added `deleteMerchantDataFromAdminPanel(request)` that calls `DELETE /api/gdpr/delete` with `{ confirm: true, permanent: true }`.
- Updated `packages/shopify-app/app/routes/app.settings.tsx`:
  - added new action intent `wipe-merchant-data` with strict confirmation phrase `DELETE ALL MERCHANT DATA`.
  - added dedicated success/error banner handling for wipe action.
  - added new `Danger zone` section with destructive form and confirmation input.
  - button stays disabled until exact confirmation phrase is entered.

## Observed Issues
- None during typecheck.

## Open Items
- Deploy this change to production if approved.
## Decisions
- Revised wipe flow semantics: keep merchant identity row, wipe operational merchant data.

## Changes
- Updated `packages/api/src/lib/dataDeletion.ts`:
  - added `clearMerchantDataKeepMerchant(merchantId)` to delete merchant-scoped operational data while preserving merchant row.
  - resets mutable merchant settings/billing flags (`persona_settings`, `notification_phone`, `guardrail_settings`, subscription fields) without deleting merchant identity.
- Updated `packages/api/src/routes/merchants.ts`:
  - added `DELETE /api/merchants/me/data-reset` (requires `{ confirm: true }`).
- Updated `packages/shopify-app/app/platform.server.ts`:
  - `deleteMerchantDataFromAdminPanel()` now calls `/api/merchants/me/data-reset`.
- Updated `packages/shopify-app/app/routes/app.settings.tsx`:
  - danger zone messaging now explicitly says merchant creation record is preserved.

## Verification
- `pnpm --filter @recete/api typecheck` passed.
- `pnpm --filter @recete/shopify-app typecheck` passed.
## Deployments
- Committed `a07c4ea` (`Add merchant data reset that preserves merchant record`).
- Pushed to `origin/main` and deployed to production (`167.172.60.234`):
  - `git pull`
  - `pnpm --filter @recete/api build`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart api web shopify-shell`
  - `pm2 list` confirmed `api`, `web`, `shopify-shell`, `workers` all `online`.
## Changes
- Performed application performance baseline measurements (build + runtime latency).
- Build timings (local):
  - `@recete/api build`: real `4.56s`
  - `@recete/workers build`: real `1.07s`
  - `@recete/shopify-app build`: real `3.64s`
  - `@recete/web build`: real `24.22s`
- External endpoint latency (public):
  - `https://recete.co.uk/health`: avg `0.243s` (10 samples), min `0.205s`, max `0.311s`
  - `https://shop.recete.co.uk/auth/login`: avg `0.224s` (10 samples), min `0.190s`, max `0.272s`
- External short load tests (`ab`, local runner):
  - `https://recete.co.uk/health` (`n=200,c=20`): `51.11 req/s`, p95 `482ms`, max `592ms`
  - `https://shop.recete.co.uk/auth/login` (`n=200,c=20`): `43.04 req/s`, p95 `793ms`, max `899ms`
- Server-side process/runtime checks:
  - `pm2 list`: `api`, `web`, `shopify-shell`, `workers` all online.
  - Internal API health port discovery: `3002` returns `200` (`3001` returns `404`).
  - Internal API latency (`http://127.0.0.1:3002/health`, 10 samples): avg `0.046s`, min `0.033s`, max `0.079s`.

## Open Items
- Full authenticated-route performance profiling (Shopify embedded/session-token flows, API merchant-scoped endpoints) still pending.
- DB-level query latency profiling (Supabase query stats/EXPLAIN for top endpoints) still pending.
## Observed Issues
- Shopify admin actions were failing with `403 Forbidden: Invalid internal secret`.
- Root cause: API -> shopify-shell internal calls used `INTERNAL_SERVICE_SECRET`, while shell internal route guards prioritized `PLATFORM_INTERNAL_SECRET` and required exact single-value match.

## Changes
- Updated `packages/shopify-app/app/routes/internal.plan.tsx` and `packages/shopify-app/app/routes/internal.billing.usage.tsx`:
  - internal secret guard now accepts either configured secret (`PLATFORM_INTERNAL_SECRET` or `INTERNAL_SERVICE_SECRET`) instead of strict priority-only single comparison.
- Verification:
  - `pnpm --filter @recete/shopify-app typecheck` passed.

## Deployments
- Committed `0864405` (`Accept both internal secrets for shell internal routes`).
- Pushed to `origin/main` and deployed to production (`167.172.60.234`):
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed all services online.
## Decisions
- Dashboard was refactored from passive metrics-first layout to guided onboarding-first layout without changing loader/API contracts.

## Changes
- Updated `packages/shopify-app/app/routes/app.dashboard.tsx`:
  - Removed duplicate primary CTA pattern (`ShellPage.primaryAction`, blocker action, next-focus action).
  - Added single dominant hero CTA: `Activate Recete` in new top section (`Start using Recete`).
  - Replaced `Next focus` with `Next step` and actionable guidance text.
  - Replaced `System status` row view with onboarding checklist (`Setup progress`) using done/pending/locked indicators.
  - Improved terminology:
    - `Consent-active users` -> `Customers who allowed messages`
    - `Catalog rows` -> `Products ready`
    - `Response rate` -> `Reply performance`
  - Added helpful zero-value metric hints and fallback `—` for reply performance when unavailable.
  - Kept existing data bindings (`subscription`, `metrics`, `recentOrders`) and route contracts intact.

## Verification
- `pnpm --filter @recete/shopify-app typecheck` passed.
## Deployments
- Committed `9a0762c` (`Refactor dashboard to activation-first onboarding flow`).
- Pushed to `origin/main` and deployed to production (`167.172.60.234`):
  - `git pull`
  - `pnpm --filter @recete/shopify-app build`
  - `pm2 restart web shopify-shell`
  - `pm2 list` confirmed all services online.
## Changes
- Refined dashboard into onboarding-first workspace with state split in `packages/shopify-app/app/routes/app.dashboard.tsx`:
  - incomplete setup state now renders `Getting started` experience with one primary CTA and dependency-aware steps.
  - merged onboarding guidance into a single current-step card (action + unlock rationale).
  - hides metrics/analytics widgets during incomplete setup and replaces with `Live dashboard preview` guidance.
  - complete setup state renders actual dashboard metrics + recent orders.
  - setup flow now tracks: plan activation -> products -> messaging config -> first order -> conversations.

## Verification
- `pnpm --filter @recete/shopify-app typecheck` passed.
