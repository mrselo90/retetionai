# 2026-09-25

## Context

- Main focus: project review ("projeyi incele"), read-only.
- Environment: local monorepo, main @ 7d1c333.

## Decisions

- User asked to install suyla.ai's WhatsApp stack (whatsmeow linked device, `/Users/sboyuk/kod/eralp-ai/wa-worker-go`) in Recete; reverses the 09-24 "not pursued". User confirmed: keep Recete-specific differences (API-owned data, restricted DB role, daily caps, IsOnWhatsApp check).

## Changes (uncommitted, not deployed)

- `packages/wa-worker` completed (Go): main.go (loopback HTTP: health/connect/status/send/disconnect, x-wa-worker-secret), sessions.go (merchant-keyed, forwards events), caps.go (daily cap 40/80/150/300 by days since first pairing, replies within 24h uncounted), forward.go (parks undelivered events in wa_session). go test passes.
- Migration `046_whatsapp_linked_device.sql`: `public.whatsapp_connections`, provider 'whatsmeow' in inbox/outbox CHECKs, outbound delivered_at/read_at, role `recete_wa_worker` (NOLOGIN, search_path wa_session), schema `wa_session`.
- `packages/shared/src/whatsappWorker.ts`: send/credentials via worker; api+workers `lib/whatsapp.ts` re-export it. Dashboard replies send `typedByPerson` (no pacing/cap).
- API: `routes/waWorker.ts` (`POST /internal/wa-worker/events`: inbound -> whatsapp_inbound_events + queue; own_message -> conversation + human handover; receipts), `routes/whatsappConnection.ts` (`/api/integrations/whatsapp/{status,connect,disconnect}`), auth allowlist. Uninstall webhook + permanent data deletion unlink the session.
- Workers: scheduled message hitting daily cap -> `moveToDelayed` to reset (+jitter), not failed.
- UI: web `WhatsAppConnectCard` (QR, polling, disclosure, en/tr), Shopify `app.whatsapp.tsx` resource route + Polaris card on Integrations; setup step "Connect WhatsApp" restored from HEAD (setupProgress, app.\_index, app.tsx, bootstrap) keyed on `available`.
- Deploy: deploy.yml builds wa-worker if Go present (never fails deploy); ecosystem adds `wa-worker` PM2 app only when binary + WA_PG_DSN + WA_WORKER_SECRET exist. Env examples updated. README with setup.
- Verified: typecheck, api 376 tests, shared 9, go test, both UI builds; local throwaway Postgres: migration idempotent, worker as restricted role gets real QR from WhatsApp, cannot read merchants, tables in wa_session, disconnect clears row. UI not visually checked (auth).

## Deployments

- None.

## Observed Issues

- Working tree is NOT the committed state. The index stages a revert of `726a547` (Embedded Signup): whatsappCloud.ts, whatsappConnect.ts, WhatsAppConnectCard, metaEmbeddedSignup deleted; api/workers `lib/whatsapp.ts` back to provider-free. Index ~= `2fab752` + some `12f3024` copy. More unstaged copy edits on top (landing, privacy, DPA, whatsapp-gdpr, en/tr). Origin unknown — not done by this session.
- Untracked `* 2.ts(x)` / `* 2.md` duplicates of the Embedded Signup files (17:07 on 09-24, pre-prettier versions) — Finder/iCloud-style copies. `packages/shared/src/whatsappCloud.test 2.ts` breaks `pnpm -r typecheck` (TS2307).
- `packages/wa-worker/` (untracked, Go) uses `go.mau.fi/whatsmeow` = unofficial WhatsApp client, which was declined on 09-24 (against WhatsApp terms).
- `packages/api/node_modules 2/` still present (Aug 13 leftover). Stale worktree `.claude/worktrees/gracious-dewdney-553191` (Aug 13).
- api tests on working tree: 367 pass / 1 skipped (was 379 with Embedded Signup tests).
- Test coverage: api 48 test files; workers, web, shopify-app have none.

## Gaps closed (later same day, user: "eksikleri gider")

- Images: worker forwards image messages with serialized ImageMessage keys (thumbnail + context stripped); new worker `POST /media` downloads + decrypts on demand (10MB cap, 410 when expired); API stores keys in inbound payload `image.providerMediaId`; `aiVision.resolveImageDataUrl` -> `fetchWhatsAppImageDataUrl`. Images never stored.
- Copy: privacy (WhatsApp/Meta sub-processor + linked-device keys paragraph), DPA, /whatsapp-gdpr (checklist step, roles, data locations), privacy snippet, landing (proof fact, feature, how-it-works, plans "Your own WhatsApp number"), Shopify billing tiers, dashboard setup step -> Integrations. Dates -> 2026-09-25. Checked /whatsapp-gdpr + landing text in browser.
- Tests: new `whatsappConnection.test.ts`; api 382 pass, shared 11, go test ok, typecheck clean, both builds pass.

## Shopify app UX review + first three fixes (later same day, uncommitted)

- Reviewed blackeagletest embedded app with Claude in Chrome (needed Chrome zoom 100%; at 80% clicks missed). Tested: setup checklist, Start, Back to setup, Integrations, Link WhatsApp -> QR -> Cancel, Billing, Dashboard (locked), Products list + detail. Typing into the iframe did not focus and hit Shopify shortcuts -> stopped text-entry tests.
- Fix 1 (product instructions): API `POST /api/products/instructions/draft` (lib/llm/draftInstructions.ts, stateless, max 10/call, JSON mode, usage tracked as product_instruction_draft); Shopify Products: "Draft all with AI" card (intent bulk-draft: drafts, saves via persistProductSetup, respects plan recipe limit, starts knowledge prep, max 30/click) + "Write it for me"/"Rewrite with AI" in product detail (useFetcher, fills form, merchant saves); skincare-only examples removed.
- Fix 2 (consistency): `isBillingReady` no longer counts "trial" (API sets trial on every install -> "Pick a plan: Done" without any Shopify plan); dashboard/analytics/customers/settings now use `shellSetupProgress` (same inputs as shell: billing approval, WhatsApp step) — dashboard said "3 required steps" vs 4; Integrations billing badge + removed "Orders flowing" reference.
- Fix 3 (buttons): Overview header "Continue setup" removed during setup; SetupTrail banner single "Next: <step>"; Dashboard locked page no buttons (banner carries CTA).
- Effect: stores on install-default "trial" without a Shopify plan now see "Pick a plan" as step 1 (accurate).
- Deployed `46a14d5` (user push; Deploy run 36104618114 success; all 5 PM2 services online). Checked live on blackeagletest: Overview 0/4 with single "Continue: Pick a plan", banner "Next: Pick a plan", Products "Draft all with AI" card, "Write it for me" filled all 3 fields in <10s (not saved), locked Dashboard "4 of 4 steps left" with no buttons, Integrations badge "No plan yet". "Draft all with AI" not run (would write 16 products to the test store).

## Remaining UX items 4-9 (later same day, uncommitted)

- 4 dead ends: Integrations "Open dashboard" only when setup complete and orders exist; Products "You're all set" dashboard/conversations buttons only when setup complete.
- 5 copy: merchant-third-person/internal copy rewritten (Settings subtitles, AI Vision gate + help, data-deletion banners, Products "Your guidance").
- 6 "Polish your setup" only after required steps (was: when hasBilling).
- 7 Integrations rewritten: WhatsApp card + one "Shopify orders" card (removed 4 counters, Operator guidance, Live connections).
- 8 loading: shell shows a skeleton instead of the old page while navigating to another path. Server timings from shopify-shell logs are fast (document 6-400 ms, bootstrap.data 400-900 ms, once 2.3 s) -> the 5-13 s blank on first open is Shopify admin/iframe/JS load, not our loaders.
- 9 Settings "Danger zone" -> "Delete store data", collapsed behind "Show data deletion".
- Deployed `979aa9d` (items 4-9) + `11683de` (removed raw "trial" badge on Settings > Bot behavior), both pushed by Claude with user OK; deploy runs success, health 200. Live check: Integrations shows only WhatsApp + "Shopify orders" cards; Settings new subtitle. Scrolling inside the iframe did not work in Claude in Chrome, so Danger-zone collapse not seen live.
- Found: shopify-shell access log writes full URLs incl. Shopify `id_token` (60 s JWT) -> should strip query strings from logs.

## Unknown-sender auto-reply fix (later same day, uncommitted)

- waWorker.ts inbound: only senders that match a customer by phone (`findUserByPhone`) are stored/queued; no phone (@lid only) -> `ignored_unknown_sender`, not a customer -> `ignored_not_a_customer`; 200 so the worker doesn't retry. Nothing stored for strangers.
- whatsappInboundProcessor.ts: provider 'whatsmeow' + unknown user -> status ignored, no "share your order number" reply (defense in depth).
- Tests: +2 (stranger ignored, @lid ignored); api 387 pass.

## Shopify app "Install Recete" page report (later same day)

- User saw auth.login page ("Get started with Recete / Go to Shopify App Store") though installed. That route renders when authenticate fails (no shop/host params). Asked user where it was opened (admin sidebar vs direct URL/new tab). Server-log check was blocked by the session safety check. Not resolved yet.

## Billing page "Free / No Plan" after paying (later same day, uncommitted)

- User approved Growth via Managed Pricing (return URL /app/billing?plan_handle=growth&charge_id=...). Checklist showed plan done (merchant status 'active' via webhook) but Billing said "Free / No Plan".
- Cause (shopify-api 12.3 check.mjs): subscription counts only if `plans` includes its name AND (isTest || !test). App passed plans=["growth-monthly",...] (Managed Pricing names differ) and isTest=false in prod (dev store charges are test).
- Fix: app.bootstrap.tsx + app.billing.tsx use `billing.check({ isTest: true })` (any active subscription). billingUsage.server.ts already reads activeSubscriptions unfiltered and maps by name substring.
- Deployed `d7b0230` (run 36107105468 success). Live: Billing shows "Active · Growth", Growth card highlighted, banner "Setup 1 of 4 done · Next: Connect WhatsApp".

## "Couldn't link device" on blackeagletest (later same day)

- Worker restarted with WA_DEBUG=1 at 12:41 UTC; turned off again at 12:47.
- Cause: other Recete account b7029296 (user's personal number, unlinked from phone 10:13) kept phone_e164 + status 'qr' (and wm_jid). merchantOwningPhone counted 'qr'/'connecting' rows -> blackeagletest (fb7635f5) pairing refused "number already linked to another store" (07:24, 12:43). Also Restore started a background fresh pairing for b7029296 ("mapped device missing, pairing fresh").
- Live fix: called worker /disconnect for b7029296 -> disconnected, phone cleared.
- Code (uncommitted, not deployed): merchantOwningPhone only status='connected'; Restore skips + wipes rows whose device keys are gone. A boot reset of interrupted pairings (stale QR after restart) was blocked by the session safety check — still open.
- Deploying worker code needs local cross-compile + scp + pm2 restart (no Go on server).
- User linked blackeagletest after the cleanup. Committed `e38169c` (not pushed); worker binary from it scp'd (sha256 ce8d6f38…), `pm2 delete` + `pm2 start ecosystem --only wa-worker` + `pm2 save` -> WA_DEBUG off, restored + connected fb7635f5 at 12:47 UTC.

## Theme extension removal + Polaris/App Bridge pass (2026-09-26, uncommitted)

- Removed repo-root `extensions/theme-extension` (only a console.log; outside the CLI app root packages/shopify-app, so likely never deployed) and the optional "Enable on-site widget" step (setupProgress key, bootstrap prisma lookup, markThemeEmbedEnabled, theme editor link). Prisma column `themeEmbedEnabled` left in schema on purpose (deploy runs `prisma db push`).
- App Store self-review list fetched via `npx @shopify/cli@latest doc fetch` (project CLI 3.91.1 lacks `doc fetch`). Skill's usage-logging step skipped (would send the user prompt to Shopify).
- Fixed 2.3.1 violation: `routes/_index` (shop.recete.co.uk) had a "Shop domain" input posting to /auth/login + custom CSS + internal copy -> rewritten in Polaris: App Store button + "Open in Shopify admin".
- 2.2.4: removed unused REST helpers (shopifyApiRequest/createWebhook/listWebhooks/deleteWebhook) from api/src/lib/shopify.ts; real calls are GraphQL.
- Polaris: WhatsApp Unlink confirm -> Polaris Modal; dead window.confirm removed; product images -> Thumbnail; QR -> Image; chat bubbles -> Box; sticky header div -> Box; ErrorBoundary -> Page; shell width div -> InlineStack+Box; SectionCard div -> Box; unused FloatingActionFeedback deleted. No raw HTML/inline styles left in shopify-app routes/components (except Polaris linkComponent <a>).
- App Bridge: CDN app-bridge.js in root head, NavMenu, session tokens — already in place. Added App Bridge SaveBar on Settings (replaces Polaris ContextualSaveBar and the header Save button) and on the messaging wizard (open when dirty, closed during post-save redirect; "Save and continue" kept). Not done: TitleBar (optional).
- Tests: api setupProgress test updated (no themeEmbed); api 387 pass; shopify typecheck/lint/build clean.

## Open Items

- Unknown-sender auto-reply: deployed `ad3f4a4` (deploy run 36106335593 success, api/workers/wa-worker online).
- PARKED (user): moving the DB from Supabase to the DO server's existing Postgres. Findings: 522 supabase-js .from() + 6 rpc (PostgREST), Supabase Auth (web login, API getUser, auth.admin, 7 migrations ref auth.users), pgvector needed; no storage/realtime. Options: A self-host PostgREST+GoTrue (days), B rewrite (weeks), C move only wa-worker off Supabase (half day, recommended first).
- App Store listing text (outside repo) still needs the linked-device wording.
- To go live: apply 046, set role password, install Go on server, root .env WA\_\* vars (check port 3005 free), deploy, restart api+workers.
- Legal copy updated but not lawyer-reviewed.
- iCloud: `.next` got new ` 2` copies during this session -> Desktop sync is live on the repo; strongly suggest moving repo out of ~/Desktop.
- Everything in 2026-09-24 Open Items still open.
