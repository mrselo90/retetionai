# 2026-04-04

## Context
- Main focus: Shopify embedded app UI/UX cleanup across Overview, Products, and Mapping pages.
- Environment: local workspace (`retention-agent-ai`), Remix + Polaris routes only.

## Decisions
- Apply only UI/render/copy changes; no backend/action/loader/API contract edits.
- Keep billing CTA phrase (`Approve billing`) only in Overview `Next step` card.
- Keep mapping modal save action only in modal footer.

## Changes
- Updated `app._index.tsx`:
  - Removed duplicate billing CTA from launch banner and recommended billing card (when pending).
  - Added active-users zero-state hint text.
  - Added response-rate context caption.
  - Reworked setup banner layout so CTA sits below body copy.
  - Added footer copy: `Want more control? Adjust bot behavior in Settings or prepare your catalog in Products.`
- Updated `app.products._index.tsx`:
  - Replaced raw step status sentence with compact 3-step visual indicator badges.
  - Removed `no run yet` fallback lines (only render outcome rows when real data exists).
  - Added CTA weighting logic so next recommended action gets primary styling for non-ready products.
  - Shortened product URL display to pathname.
  - Removed `Optional actions` label while keeping details toggle.
  - Added active-step styling for `Start Here` cards based on pipeline counts.
  - Fixed grammar: `Can't find a product in Shopify mapping?`
- Updated `app.products.mapping.tsx`:
  - Removed mapping queue header `Mapped` badge.
  - Changed plan metric label to `Mapped products` and hint to `X of Y on current plan`.
  - Moved modal `Back to queue` into footer (left), kept `Close` and `Save` on right.
  - Removed duplicate save button from modal body.
  - Converted disabled Shopify status/handle fields to read-only text rows.
  - Removed redundant textarea help text under Customer guidance.
  - Changed linked-state chip to non-interactive `Badge` with `✓ Local record linked`.

## Deployments
- None in this pass.

## Observed Issues
- Minor Polaris token/type mismatches fixed during typecheck (background token + missing import).

## Open Items
- Validate final visual behavior in embedded Shopify UI on real merchant session.

## Deployments
- Production deploy completed for Shopify UI/UX fixes.
- Synced files:
  - `packages/shopify-app/app/routes/app._index.tsx`
  - `packages/shopify-app/app/routes/app.products._index.tsx`
  - `packages/shopify-app/app/routes/app.products.mapping.tsx`
- Production commands:
  - `cd /root/retetionai/packages/shopify-app && pnpm build`
  - `pm2 restart shopify-shell`
  - `pm2 status shopify-shell`
- Result: `shopify-shell` online after restart.
