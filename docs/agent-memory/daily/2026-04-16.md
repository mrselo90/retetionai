# YYYY-MM-DD

## Context
- Main focus: investigate setup flow still sticking at `2/4` after billing + product + settings flow in Shopify test store.
- Environment: local monorepo and production server `167.172.60.234`.

## Decisions
- Keep the shared 4-step setup flow (`billing`, `products`, `messaging`, `orders`).
- Fix the remaining “stuck” behavior by invalidating cached merchant overview data immediately after setup-relevant mutations, instead of relying on the 15-second TTL.

## Changes
- Updated `packages/api/src/lib/cache.ts`:
  - added `invalidateMerchantOverviewCache(merchantId)` to clear `shopify-merchant-overview:*` API cache entries for a merchant.
- Updated `packages/api/src/routes/merchants.ts`:
  - invalidate merchant overview cache after `PUT /api/merchants/me`.
  - invalidate merchant overview cache after `DELETE /api/merchants/me/data-reset`.
- Updated `packages/api/src/routes/products.ts`:
  - setup-related product mutations now also invalidate merchant overview cache through `invalidateProductKnowledgeCaches(...)`.

## Deployments
- Pending deploy.

## Observed Issues
- Launch progress could remain visually stuck at `2/4` even after saving settings because the shell was polling cached merchant overview data that was not invalidated by merchant/settings or product mutations.

## Open Items
- Deploy the cache invalidation fix to production and re-check the live test-store flow.
