# 2026-04-13

## Context
- Main focus: Super Admin automated Shopify scenario testing coverage.
- Environment: local monorepo (`packages/api`, `packages/web`).

## Decisions
- Reused existing super-admin test-kit architecture instead of creating a separate test system.
- Added a catalog-driven Shopify scenario runner so UI and backend stay in sync.
- Kept assertions deterministic (non-empty reply, intent family, guardrail expectation, forbidden phrase checks) to reduce flaky AI-test failures.

## Changes
- API: added Shopify scenario catalog and runner endpoints in `packages/api/src/routes/admin.ts`.
- API: implemented built-in Shopify scenario definitions for usage guidance, numeric product pick, routine request, return intent, opt-out, and multilingual flows.
- API: runner now creates delivered test order events, executes scenarios through real `generateAIResponse`, and returns pass/fail assertion detail.
- Web: rebuilt `packages/web/app/[locale]/admin/testing/page.tsx` to include scenario selection and one-click suite execution.
- Web: added pass/fail summary and per-scenario assertion reporting in Super Admin UI.
- Docs: added `docs/testing/SHOPIFY_SUPERADMIN_SCENARIOS.md` as canonical Shopify suite coverage map.
- Docs: added `docs/testing/FULL_APPLICATION_TEST_SCENARIOS.md` as full-system feature test matrix (auth, products, AI, Shopify, WhatsApp, billing, GDPR, admin, workers, observability).
- Tests: added `packages/api/src/test/integration/admin.test.ts` to cover:
  - `GET /api/admin/test-kit/shopify-scenarios`
  - `POST /api/admin/test-kit/shopify-scenarios/run` (validation + happy path)
- Tests: extended `packages/api/src/test/integration/products.test.ts` with enrichment-pipeline coverage:
  - `POST /api/products/:id/enrich-from-url`
  - `POST /api/products/:id/prepare-knowledge`
  - default enrichment mock setup to keep scrape-path tests on success path
- Tooling: added root test-flow scripts in `package.json`:
  - `test:flow` (typecheck + unit + integration)
  - `test:flow:full` (core flow + Playwright smoke)
  - `test:e2e:smoke` (Chromium-only auth/dashboard smoke)
- Docs: added `docs/testing/TEST_FLOW.md` with API-first test strategy and command usage.
- Test maintenance: fixed existing broken API tests so the consolidated flow is green:
  - `packages/api/src/lib/aiAgent.test.ts`
  - `packages/api/src/lib/groundedAnswer.test.ts`
  - `packages/api/src/middleware/rateLimit.test.ts`
  - `packages/api/src/test/integration/products.test.ts`
- WhatsApp routine-quality bugfix:
  - Updated `packages/api/src/lib/aiAgent.ts` to treat routine-building as first-class intent.
  - Added standalone routine-intent detection (`daily routine`, `use together`, `all products`, etc.).
  - Added continuation detection for replies like `"For all of them"` when previous assistant turn asked product selection.
  - Prevented unnecessary clarification branch in routine-all-products flows.
  - Added best-effort routine builder (morning/evening + excluded-product note for mixed orders) when order context exists but evidence is weak.
- Escalation false-positive hardening:
  - Updated `packages/api/src/lib/guardrails.ts` to use unicode-aware boundary matching for keywords.
  - Tightened AI-response crisis detection to high-severity cues only.
  - Stopped automatic human escalation on AI-output crisis matches (now block/clarify only) to avoid routine/fallback loops escalating as crisis.
- Added tests:
  - `packages/api/src/lib/aiAgent.routine.test.ts`
  - new post-delivery signal tests in `packages/api/src/lib/aiAgent.test.ts`
  - new guardrail anti-false-positive tests in `packages/api/src/lib/guardrails.test.ts`

## Verification
- Ran `pnpm test:flow` successfully.
- Result:
  - API tests: 282 passed
  - Shared tests: 2 passed
  - Integration tests: 31 passed
- Re-ran integration verification after enrichment test additions:
  - `pnpm test:integration` => 33 passed
  - `products.test.ts` => 12 passed
- Routine fix verification:
  - `pnpm --filter @recete/api test src/lib/aiAgent.test.ts src/lib/aiAgent.routine.test.ts src/lib/guardrails.test.ts` => passed
  - `pnpm --filter @recete/api test` => 42 files, 292 tests passed
  - `pnpm --filter @recete/api typecheck` => passed

## Deployments
- None in this session.

## Observed Issues
- Initial frontend rewrite had shell-quoting corruption; file was rewritten cleanly.
- Typecheck errors fixed:
  - implicit `any` in admin scenario-id parsing
  - Polaris button label type mismatch with numeric interpolation

## Open Items
- Consider extending scenario runner to include explicit Shopify OAuth/webhook health checks where environment credentials are available.
- Add end-to-end webhook/inbound processor test for routine continuation (`"For all of them"`) through `processWhatsAppInboundEvent`.

## Additional Changes (WhatsApp robustness hardening)
- `packages/api/src/lib/aiAgent.ts`
  - Added explicit `ConversationFlowState` (`current_intent`, `selected_products`, `last_question_type`) reconstruction from conversation context.
  - Added `UserGoal` abstraction + `inferUserGoal` (LLM-first, heuristic fallback) to reduce keyword-only routing and unify routine intent handling.
  - Added deterministic continuation resolution for `"For all of them"` via explicit state (`last_question_type` + selected all products).
  - Added routine compatibility classification pipeline (`classifyRoutineProducts`) with LLM classification and deterministic fallback.
  - Added mixed-order filtering to exclude hygiene/irrelevant items from skincare routine generation.
  - Added routine response quality validator (`validateRoutineAnswerQuality`) and strict regeneration pass when quality fails.
  - Added best-effort routine fallback with compatibility/exclusion notes when generation remains weak.
  - Added observability logs for:
    - fallback trigger + reason
    - routine builder usage + included/excluded products
    - escalation trigger + reason
    - crisis precheck matched but rejected by layered classifier
- `packages/api/src/lib/guardrails.ts`
  - Added layered crisis architecture:
    - `evaluateCrisisEscalation` (keyword precheck + LLM confirmation, high-severity override)
    - `checkUserMessageGuardrailsWithoutCrisis` to separate medical/custom checks from crisis decisioning
  - Existing `checkUserMessageGuardrails` kept for compatibility.
- Tests:
  - Updated `packages/api/src/lib/aiAgent.test.ts` and `packages/api/src/lib/aiAgent.routine.test.ts` guardrail mocks to new layered APIs.
  - Extended `packages/api/src/lib/guardrails.test.ts` with layered-crisis and non-crisis guardrail-path tests.

## Additional Verification
- `pnpm --filter @recete/api typecheck` => passed
- `pnpm --filter @recete/api test src/lib/aiAgent.test.ts src/lib/aiAgent.routine.test.ts src/lib/guardrails.test.ts` => passed
- `pnpm --filter @recete/api test` => passed (`42` files, `295` tests)

## Additional Changes (Config-aware conversational intelligence)
- `packages/api/src/lib/aiAgent.ts`
  - Added persona-aware response post-policy (`applyPersonaResponsePolicy`) to enforce merchant settings on all runtime response branches:
    - emoji off => strips emojis (including handoff/escalation/static fallback text)
    - response length short/medium => conservative output length caps for deterministic/static branches
    - minor tone normalization for deterministic/static branches
  - Added unified finalizer (`finalizeConfiguredResponse`) so fallback-language notice + persona constraints are applied consistently.
  - Strengthened explicit conversation state:
    - existing: `current_intent`, `selected_products`, `last_question_type`
    - new: `for_whom`, `preferred_routine_scope`, `simplicity_preference`, `unresolved_clarification_need`
  - Added contextual continuation helper for short replies (`all`, `both`, `together`, `same routine`, `morning only`, `evening only`) using state instead of isolated keyword handling.
  - Made state interpretation memory-mode-aware:
    - uses memory settings (`full` vs `last_n`) for context window in continuation/state inference.
  - Kept routine builder path best-effort and deterministic, now with persona + scope/simplicity support.
  - Improved observability with detailed logs for fallback reasons, routine builder inclusion/exclusion, escalation reason, and crisis precheck rejections.
- Tests:
  - Expanded `packages/api/src/lib/aiAgent.routine.test.ts` with:
    - formal tone + short response effect
    - language restriction fallback notice
    - limited memory mode continuation resolution
    - human handoff with emoji disabled and enabled
  - Existing layered crisis tests remain in `guardrails.test.ts`.

## Additional Verification
- `pnpm --filter @recete/api typecheck` => passed
- `pnpm --filter @recete/api test src/lib/aiAgent.test.ts src/lib/aiAgent.routine.test.ts src/lib/guardrails.test.ts` => passed (`31` tests)
- `pnpm --filter @recete/api test` => passed (`42` files, `300` tests)

## Additional Changes (Conversation-quality pass)
- Added transcript-style quality fixtures in `packages/api/src/lib/aiAgent.conversationQuality.test.ts`:
  - short contextual reply (`all of them`)
  - vague/constraint follow-up (`morning only`, `keep it simple`)
  - indirect request (`for my wife`, `just tell me the order`)
  - mixed-language + reply-language restriction behavior
  - tone/length/emoji behavior on deterministic responses
  - harmless routine non-crisis vs real high-severity crisis escalation
- Conversation behavior refinements in `packages/api/src/lib/aiAgent.ts`:
  - Improved contextual continuation detection for natural short replies with extra words/punctuation.
  - Added routine format preference (`step_order` vs morning/evening sections).
  - Best-effort routine now adapts output by context:
    - optional target-person phrasing (`for your wife/husband`)
    - optional `Just the order` format
    - scope-aware morning/evening inclusion
    - simplicity-aware concise output
  - Maintained admin-configured tone/length/emoji/language handling via response finalizer.

## Additional Verification
- `pnpm --filter @recete/api test src/lib/aiAgent.conversationQuality.test.ts src/lib/aiAgent.routine.test.ts` => passed (`13` tests)
- `pnpm --filter @recete/api typecheck` => passed
- `pnpm --filter @recete/api test` => passed (`43` files, `307` tests)

## Additional Changes (Pilot-readiness pass)
- Added rollout counters in `packages/api/src/lib/metrics.ts`:
  - `routine_intent_detected_count`
  - `contextual_continuation_resolved_count`
  - `clarification_asked_count`
  - `best_effort_routine_used_count`
  - `routine_quality_regenerated_count`
  - `fallback_triggered_count{reason=...}`
  - `crisis_precheck_count`
  - `crisis_confirmed_count`
  - `crisis_rejected_count`
  - `human_handoff_requested_count`
- Added sanitized pilot transcript diagnostics capture:
  - new file `packages/api/src/lib/assistantPilotDiagnostics.ts`
  - captures hashed + previewed message/response and per-response diagnostics metadata
  - logs structured `assistant_pilot_diagnostic` entries and stores records in `ai_usage_events` (`feature=assistant_pilot_diagnostic`)
- Extended `packages/api/src/lib/aiAgent.ts` instrumentation:
  - metrics increments for intent/continuation/clarification/fallback/crisis/handoff paths
  - per-response diagnostics include:
    - inferred goal
    - selected/excluded products
    - merchant behavior settings snapshot
    - memory mode/count
    - generation mode (`generated|regenerated|fallback`)
    - fallback reason
    - escalation decision/reason
- Added pilot review endpoints in `packages/api/src/routes/admin.ts`:
  - `GET /api/admin/pilot/diagnostics`
  - `GET /api/admin/pilot/review-template`
- Added pilot monitoring guide:
  - `docs/testing/PILOT_ROLLOUT_MONITORING.md`
- Added/updated tests:
  - `packages/api/src/test/integration/admin.test.ts` now covers pilot diagnostics + review-template endpoints.

## Additional Verification
- `pnpm --filter @recete/api test src/test/integration/admin.test.ts src/lib/aiAgent.conversationQuality.test.ts src/lib/aiAgent.routine.test.ts src/lib/aiAgent.test.ts src/lib/guardrails.test.ts` => passed (`43` tests)
- `pnpm --filter @recete/api typecheck` => passed
- `pnpm --filter @recete/api test` => passed (`43` files, `309` tests)

## Additional Changes (Strict memory + sticky order context)
- Implemented persistent structured conversation-state usage in `packages/api/src/lib/aiAgent.ts` (loaded and updated on every branch via `getConversationStructuredState` / `updateConversationStructuredState`).
- Enforced effective order continuity with fallback to structured state (`effectiveOrderId = runtime orderId || state.order_id`).
- Added sticky known-order-products behavior:
  - hydrate from persisted state at request start
  - refresh from order scope fetches
  - persist back to structured state
  - reuse when branch changes or orderId is omitted in later turns.
- Added explicit full-memory handling helper (`buildModelHistory`) with:
  - full mode => include full usable history
  - explicit truncation only when payload exceeds safety cap
  - truncation observability in logs + pilot diagnostics.
- Added defensive anti-regression for redundant product prompts:
  - detect responses that ask customer again which products they bought/used
  - if known products exist, replace with known-products recovery guidance (or routine best-effort response).
- Injected structured state snapshot context into LLM prompts/runtime hints to preserve context in limited-memory operation.
- Added DB migration `supabase/migrations/039_conversation_context_state.sql` to add `conversations.conversation_context JSONB`.
- Updated tests/mocks for new state APIs:
  - `packages/api/src/lib/aiAgent.test.ts`
  - `packages/api/src/lib/aiAgent.routine.test.ts`
  - `packages/api/src/lib/aiAgent.conversationQuality.test.ts`
- Added new test suite: `packages/api/src/lib/aiAgent.memoryState.test.ts` covering:
  - products shown early reused later
  - delivery detour then back to usage
  - full memory mode history inclusion
  - limited memory mode with sticky product context
  - prevention of redundant product questions
  - fallback branch retaining known products.

## Additional Verification
- `pnpm --filter @recete/api typecheck` => passed
- `pnpm --filter @recete/api test src/lib/aiAgent.memoryState.test.ts src/lib/aiAgent.routine.test.ts src/lib/aiAgent.conversationQuality.test.ts src/lib/aiAgent.test.ts` => passed (`29` tests)
- `pnpm --filter @recete/api test` => passed (`44` files, `315` tests)

## Deployment (Production)
- Deployed commit `3d31fa2` to production server `167.172.60.234`.
- Before deploy, server worktree had local edits in:
  - `packages/api/src/lib/aiAgent.ts`
  - `packages/api/src/lib/i18n.ts`
  - `packages/shopify-app/app/routes/app.products._index.tsx`
  - plus untracked `packages/shopify-app/build_error.txt`
- Preserved server-local edits with stash:
  - `stash@{0}: pre-deploy-2026-04-13`
- Ran server deploy script: `cd /root/retetionai && ./deploy.sh`
  - Pulled latest `main`
  - Installed dependencies
  - Built all packages successfully
  - Restarted PM2 services (`api`, `web`, `shopify-shell`, `workers`)
- Applied DB migration on production:
  - `supabase/migrations/039_conversation_context_state.sql`
  - Verified column exists: `conversations.conversation_context` (`jsonb`)
- Post-deploy verification:
  - PM2: all four services `online`
  - `http://127.0.0.1:3001/` => `200`
  - `http://127.0.0.1:3003/app/health` => `200`
  - `http://127.0.0.1:3002/api/health` => `404` (route not present; process responds)
  - workers log shows successful Redis ready + inbound jobs processing after restart.
- Noted pre-existing/non-blocking log noise remains:
  - placeholder Sentry DSN warning
  - historical Next server-action stale request errors
  - historical decrypt/scheduler errors from prior sessions

## Additional Changes (Product awareness + strict resolution)
- Tightened product resolution in `packages/api/src/lib/aiAgent.ts` to reduce false mappings and prevent wrong-product substitutions:
  - added conservative fuzzy matcher (`diceSimilarity` over trigrams) with strict thresholds
  - retained exact and index-based matching
  - added all-products reference detection (`my products`, `these`, `them`, `all of them`)
  - unknown/uncertain product references now trigger safe fallback with explicit known-order product list and structured selection guidance.
- Removed weak generic clarification behavior in known-product contexts; assistant no longer asks generic "which product?" when order products are already known.
- Added defensive replacement for weak product re-ask phrasing in generated outputs.
- Added helper `buildUnknownProductSafeFallback` and routed uncertain/unknown product mentions to this response instead of potentially incorrect single-product mapping.

## Additional Tests
- Added `packages/api/src/lib/aiAgent.productResolution.test.ts` covering:
  - multi-product awareness on generic usage prompt (no redundant re-ask)
  - index selection mapping to the correct product
  - unknown product safe fallback without substitution
  - no wrong-product response path for unknown references.

## Additional Verification
- `pnpm --filter @recete/api typecheck` => passed
- `pnpm --filter @recete/api test src/lib/aiAgent.productResolution.test.ts src/lib/aiAgent.memoryState.test.ts src/lib/aiAgent.routine.test.ts` => passed (`16` tests)
- `pnpm --filter @recete/api test` => passed (`45` files, `319` tests)

## Additional Hardening (End-to-end conversation intelligence)
- Extended structured conversation state model in `packages/api/src/lib/conversation.ts`:
  - added `current_goal`
  - added `unresolved_clarification_need`
- Updated `packages/api/src/lib/aiAgent.ts` to use/persist enriched structured state fields through core branches.
- Improved deterministic response finalization consistency:
  - ensured known-product recovery responses pass through full `finalizeConfiguredResponse` (tone/length/emoji/language policy) instead of bypassing settings.
- Strengthened contextual resolution:
  - added ordinal selection parsing (`first/second/third`, etc.) for product picks
  - extended all-products references (`my products`, `these`, `them`) handling.
- Added strict-safe product fallback behavior for uncertain/unknown matches without substitution and with explicit known product list guidance.

## Additional Tests/Verification
- Extended `packages/api/src/lib/aiAgent.productResolution.test.ts` with:
  - `these/them` full-order resolution
  - ordinal selection (`the second one`) mapping
- Verification:
  - `pnpm --filter @recete/api typecheck` => passed
  - `pnpm --filter @recete/api test src/lib/aiAgent.productResolution.test.ts src/lib/aiAgent.conversationQuality.test.ts src/lib/aiAgent.memoryState.test.ts src/lib/aiAgent.routine.test.ts` => passed (`25` tests)
  - `pnpm --filter @recete/api test` => passed (`45` files, `321` tests)

## Deployment (Production update)
- Deployed commit `e7554b4` to production server `167.172.60.234`.
- Server deploy run:
  - `cd /root/retetionai && ./deploy.sh`
  - pull/install/build/restart completed successfully.
- PM2 after deploy:
  - `api`, `web`, `shopify-shell`, `workers` all `online`.
- Direct health checks:
  - `http://127.0.0.1:3001/` => `200`
  - `http://127.0.0.1:3003/app/health` => `200`
  - `http://127.0.0.1:3002/api/health` => `404` (endpoint not defined; API process responds).

## Additional Changes (Shopify install 403 hardening)
- Investigated reported error: `Request failed (403) Forbidden: Invalid internal secret` during embedded Shopify install flow.
- Root cause pattern:
  - during fresh merchant bootstrap, platform merchant endpoints can briefly return 403/404 before merchant linkage converges;
  - `app.bootstrap` data route previously propagated this as hard failure to UI.
- Code change:
  - `packages/shopify-app/app/routes/app.bootstrap.tsx`
  - added install-time pending recovery for 403/404, returning `202` with `pending: true` and a safe placeholder overview payload so shell can continue loading instead of failing.
- Commit:
  - `92e01a9` (`Handle install-time bootstrap 403 as pending state`)

## Deployment (Production hotfix)
- Deployed commit `92e01a9` to production server `167.172.60.234` via `./deploy.sh`.
- PM2 services after deploy:
  - `api`, `web`, `shopify-shell`, `workers` all `online`.
- Build verification:
  - `pnpm --filter @recete/shopify-app build` passed locally.
  - Full server build + PM2 reload completed successfully during deploy.

## Open Items
- Re-test app install flow on affected test store (`receteshop.myshopify.com`) to confirm UI now receives pending bootstrap instead of hard 403 failure.
- Optional next hardening: improve API auth error semantics for internal merchant routes so invalid/early merchant linkage does not surface as `Invalid internal secret` text when bearer auth is present but merchant record is not ready yet.
