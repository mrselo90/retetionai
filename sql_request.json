{
  "query": "ALTER TABLE public.products ADD COLUMN IF NOT EXISTS enriched_text text; COMMENT ON COLUMN public.products.enriched_text IS 'LLM-enriched product description optimized for RAG embeddings';"
}
